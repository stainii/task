[%autowidth.stretch, cols="h,a"]
|===
|Base package
|`be.stijnhooft.task.backend.migration`
|Spring components
|_Configuration properties_

* `b.s.t.b.m.MigrationProperties`

_Others_

* `b.s.t.b.m.MigrationRunner`
|Bean references
|* `b.s.t.b.t.TaskImport` (in Task)
* `b.s.t.b.t.TaskTemplateImport` (in Template)
|Properties
|* `task.migration.deployments` -- `java.util.List<be.stijnhooft.task.backend.migration.MigrationProperties$Deployment>`. the four restored `portal-recurring-tasks` databases. The `name` of each must be the **deployment name** as it appears in `subscription.origin`, not the database name — they differ in case, and for social they differ altogether
* `task.migration.mongo-database` -- `java.lang.String`. the database name inside that dump. `todo` — **not** `portal-todo`, which is the correction [#35](https://github.com/stainii/task/issues/35) made to DB-001's inferred name
* `task.migration.mongo-uri` -- `java.lang.String`. connection string for the restored `todo` Mongo database
|===
